dsa roadmap for month

day 1 - First 50 prime number,
	Prime number between 100 to 200,
	Check whether number is palindrome or not,
	Check whether two strings are anagram or not,
	Fibonacci series up to 50 using recursion.

day 2 - First 20 armstrong number,
	Identify the given number is paliprime or not,
	  " ******* " Make this pattern
		   *****
			  ***
			   *,
	Check given number is perfect number or not,
	Sum of digit until single digit.

day 3 - Find GCD (HCF) of Two Numbers,
	Count Frequency of Characters in String,
	Check if Two Numbers are Amicable,
	Given an array and a target, return indices of two numbers whose sum = target.
		nums = [2,7,11,15], target = 9
		Output: [0,1],
	Move Zeroes to End
		Input: [0,1,0,3,12]
		Output: [1,3,12,0,0]


day 4 - Longest Substring Without Repeating Characters
				Given a string s, find the length of the longest substring without repeating characters.

				Example:

				Input: "abcabcbb"
				Output: 3  ("abc")

      Container With Most Water
			Problem:
			Given an array height, find two lines that together form a container holding the most water.

			Example:

			Input: [1,8,6,2,5,4,8,3,7]
			Output: 49

			Product of Array Except Self
			Problem:
			Return an array where each element is the product of all other elements except itself.

			Constraints:

			No division allowed
			O(n) time

			Example:

			Input: [1,2,3,4]
			Output: [24,12,8,6]

			Problem:
			Check if a string containing ()[]{} is valid.

			Input: "()[]{}"
			Output: True

			Problem:
			Group words that are anagrams.

			Input: ["eat","tea","tan","ate","nat","bat"]
			Output: [["eat","tea","ate"],["tan","nat"],["bat"]]